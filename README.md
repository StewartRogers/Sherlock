# Sherlock

> A Node.js / TypeScript project. Replace this line with a one-sentence description of what Sherlock does.

## Status

Early scaffolding — the repository is set up but no application code has been added yet.

## Requirements

- Node.js 20 or newer
- npm (or pnpm / yarn)

## Getting Started

```bash
git clone https://github.com/StewartRogers/Sherlock.git
cd Sherlock
npm install
```

Once a `package.json` exists, the usual scripts will live here:

```bash
npm run dev     # start in development
npm test        # run the test suite
npm run build   # produce a production build
```

## Configuration

Copy `.env.example` to `.env` and fill in the values. `.env` files are gitignored and should never be committed.

## Project Structure

```
.
├── LICENSE
├── README.md
└── .gitignore
```

## Contributing

Issues and pull requests are welcome. Please open an issue to discuss substantial changes before submitting a PR.

## License

Released under the [MIT License](LICENSE).
