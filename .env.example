# The passcode you paste in to get into the app. Make it long and random —
# this is the only thing standing between the public and the casefiles.
APP_PASSCODE=

# Secret used to sign the session cookie. Any long random string.
# Generate one with: openssl rand -base64 32
AUTH_SECRET=
